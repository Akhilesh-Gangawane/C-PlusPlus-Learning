#include <iostream>
using namespace std;

int main(){
    int x=6;
    int y=7;

    int z= x & y;
    // it first conver the numebr in binary and then compare the 0's and 1's if gives output based on the operators used.
    cout<<z<<endl;
    return 0;
}